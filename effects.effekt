module reactive/effects

import reactive/types
import immutable/option

effect Async {
    def await(poll: => Option[EventData] at {}): EventData
}

effect Scheduling {
    def done(): Unit
    def tick(): Unit
}