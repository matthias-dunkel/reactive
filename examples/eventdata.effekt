import reactive/asyncTask
import reactive/scheduling
import reactive/types
import reactive/effects
import reactive/internal

def main(): EventData = run{asyncTask{
    val r = do await(waitTicks(2))
    r match {
        case _ => Base()
}}}


def unsafeToInt(e: EventData): Int  = e match {
    case IntData(i) => i
    case _ => do raise(RunTimeError(), "Not IntData.")
}