import reactive/types
import reactive/scheduling  
import reactive/effects

def main() = {
    run{
        oneIsDone{
            do await(fun(){Some(Base())})
            println("a")
            do await(fun(){Some(Base())})
            println("b")
        }{
            do await(fun(){Some(Base())})
            println("1")
            do await(fun(){Some(Base())})
            println("2")
        }
    }
}