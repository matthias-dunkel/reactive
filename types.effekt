module reactive/types
import immutable/option

type EventData {
    Base()
    IntData(n: Int)
    StringData(text: String)
    Error(msg: String)
}

type Either[T1, T2]{
    Left(t: T1)
    Right(t: T2)
}

type Poll = => Option[EventData] at {io}